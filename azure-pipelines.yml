name: MyPipeline

## Configuraciones de la canalización
trigger: none

pool: Default
#pool: Azure Pipelines

variables:
  my-var1: 'this is var 1'

stages:
  - template: tmpl_build_stage.yml
    parameters:
      param1: $(my-var1)
  
  - stage: DeployApp
    displayName: Apps Deployment 
    jobs:
      - job: MyDeploymentJob
        displayName: My deploying job with var $(my-var1)
        steps:
          - script: echo deploying this app from master branch
            displayName: deploy frontend app
